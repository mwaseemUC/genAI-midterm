streamlit>=1.40,<2
langchain-core>=0.3,<2
langchain-openai>=0.2,<1
langchain-chroma>=0.1,<1
chromadb>=0.5,<2
tiktoken>=0.7,<1
python-dotenv>=1.0,<2
pytest>=8,<9

# Streamlit Cloud's Linux image can ship sqlite3 < 3.35, which chromadb refuses
# to start on. Linux-only so local Windows installs are unaffected.
pysqlite3-binary>=0.5; sys_platform == "linux"
